# Saved webpage artifacts (research input, not skill content).
# Raw HTML carries third-party tracking scripts that trip the security scan
# at CRITICAL severity; keep extracted doctrine as markdown in reference/ and
# research/ instead. Add a new artifact here before saving it into research/.
research/*.html
research/*_files/
