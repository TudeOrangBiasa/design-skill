# Local eval API key — copy to .eval-key.env and fill in.
# .eval-key.env is gitignored; this example is committed for maintainers.
# Default backend: opencode zen free API (see agent-skills-eval.yaml).
EVAL_API_KEY=sk-your-opencode-zen-key
